#include "GridParser.h"
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

#include "Grid.h"


Grid* GridParser::parse(const char *filename) {
	std::ifstream input(filename);
	while(input.good()) {

	}
	input.close();
	return NULL;
}
